/* Mengambil font modern agar tampilan profesional  */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap');

:root {
    --primary: #6366f1;    /* Indigo */
    --secondary: #ec4899;  /* Pink */
    --accent: #f59e0b;    /* Amber untuk Hint */
    --bg: #f8fafc;
    --text: #1e293b;
    --white: #ffffff;
}

body {
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); /* Gradasi sesuai tema SMK [cite: 13, 15] */
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    color: var(--text);
}

.game-container {
    background: var(--white);
    width: 90%;
    max-width: 420px;
    padding: 35px;
    border-radius: 30px;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2);
    text-align: center;
}

h1 {
    font-weight: 800;
    font-size: 1.8rem;
    letter-spacing: -1px;
    margin-bottom: 5px;
    background: linear-gradient(to right, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.sub-title {
    font-size: 0.8rem;
    color: #94a3b8;
    margin-bottom: 25px;
}

/* Kotak Petunjuk (Hint) agar user mudah bermain  */
.hint-box {
    background: #fffbeb;
    border: 1px dashed var(--accent);
    padding: 15px;
    border-radius: 15px;
    margin-bottom: 25px;
    text-align: left;
}

.hint-box p {
    margin: 5px 0 0 0;
    font-size: 0.9rem;
    color: #92400e;
    line-height: 1.5;
}

/* Tampilan Garis Bawah (_) sesuai alur flowchart [cite: 46, 75] */
.kata-container {
    font-size: 2.2rem;
    font-weight: 700;
    letter-spacing: 8px;
    margin: 30px 0;
    color: var(--primary);
    min-height: 60px;
}

/* Area Input & Tombol Tebak [cite: 70, 76] */
.input-section {
    display: flex;
    gap: 12px;
    justify-content: center;
    margin-bottom: 30px;
}

input {
    width: 55px;
    height: 55px;
    font-size: 1.5rem;
    font-weight: 600;
    text-align: center;
    text-transform: uppercase;
    border: 2px solid #e2e8f0;
    border-radius: 15px;
    outline: none;
    transition: all 0.2s;
}

input:focus {
    border-color: var(--primary);
    box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.1);
}

button {
    padding: 0 25px;
    background: var(--primary);
    color: white;
    border: none;
    border-radius: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s;
}

button:hover {
    background: #4f46e5;
    transform: translateY(-2px);
}

/* Statistik Grid: Nyawa & Riwayat [cite: 85] */
.stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
}

.stat-box {
    background: #f1f5f9;
    padding: 12px;
    border-radius: 15px;
}

.stat-label {
    display: block;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    color: #64748b;
    margin-bottom: 4px;
}

.stat-value {
    font-weight: 700;
    font-size: 1.1rem;
}

#sisa-kesempatan {
    color: #ef4444; /* Merah untuk nyawa [cite: 82] */
}

/* Modal Estetik dengan Backdrop Blur [cite: 83] */
.modal {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(15, 23, 42, 0.7);
    backdrop-filter: blur(10px);
    justify-content: center;
    align-items: center;
    z-index: 100;
}

.modal-content {
    background: white;
    padding: 40px;
    border-radius: 25px;
    text-align: center;
    width: 85%;
    max-width: 320px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
}

.modal-content h2 {
    margin-top: 0;
    color: var(--primary);
}
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --blue:      #2563EB;
      --blue-lt:   #60A5FA;
      --blue-mid:  #3B82F6;
      --bg:        #0B1629;
      --bg2:       #0F1E36;
      --bg3:       #162543;
      --text:      #E2EEFF;
      --text-dim:  #6B90C0;
      --white:     #fff;
    }

    html { scroll-behavior: smooth; }
    body { font-family: 'Sora', sans-serif; background: var(--bg); color: var(--text); overflow-x: hidden; }

    /* ── NAVBAR ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 16px 56px;
      transition: background .4s, box-shadow .4s;
    }
    nav.scrolled { background: rgba(11,22,41,.95); backdrop-filter: blur(14px); box-shadow: 0 1px 0 rgba(59,130,246,.15); }
    .nav-logo { display: flex; align-items: center; gap: 10px; text-decoration: none; }
    .nav-logo img { height: 44px; width: auto; }
    .nav-links { display: flex; gap: 28px; list-style: none; }
    .nav-links a { color: var(--text); text-decoration: none; font-size: .82rem; font-weight: 500; opacity: .7; transition: opacity .2s, color .2s; }
    .nav-links a:hover { opacity: 1; color: var(--blue-lt); }
    .nav-cta {
      background: var(--blue); color: var(--white);
      padding: 10px 22px; border-radius: 8px;
      font-size: .8rem; font-weight: 600;
      text-decoration: none; transition: background .2s, transform .15s;
      box-shadow: 0 4px 14px rgba(37,99,235,.4);
    }
    .nav-cta:hover { background: var(--blue-mid); transform: translateY(-1px); }

    /* ── HERO ── */
    #hero {
      min-height: 100vh;
      display: flex; flex-direction: column; justify-content: center;
      padding: 120px 56px 80px;
      position: relative; overflow: hidden;
    }
    /* tugu silhouette SVG background */
    .hero-bg {
      position: absolute; inset: 0;
      background:
        radial-gradient(ellipse 70% 60% at 65% 40%, rgba(37,99,235,.22) 0%, transparent 65%),
        radial-gradient(ellipse 40% 50% at 15% 85%, rgba(59,130,246,.1) 0%, transparent 60%),
        linear-gradient(170deg, #0B1629 0%, #0e1d38 55%, #0B1629 100%);
    }
    /* dot grid */
    .hero-bg::before {
      content: '';
      position: absolute; inset: 0;
      background-image: radial-gradient(rgba(59,130,246,.15) 1px, transparent 1px);
      background-size: 30px 30px;
      mask-image: radial-gradient(ellipse 75% 75% at 65% 35%, black 20%, transparent 75%);
    }
    /* tugu silhouette on right */
    .tugu-deco {
      position: absolute; right: -20px; bottom: 0; top: 0;
      width: 340px;
      display: flex; align-items: flex-end;
      opacity: .07;
      pointer-events: none;
      z-index: 1;
    }
    .tugu-deco svg { width: 100%; height: auto; }

    .hero-content { position: relative; z-index: 2; max-width: 680px; }
    .hero-tag {
      display: inline-flex; align-items: center; gap: 8px;
      background: rgba(37,99,235,.15);
      border: 1px solid rgba(59,130,246,.35);
      border-radius: 99px; padding: 6px 16px;
      font-size: .72rem; font-weight: 600; letter-spacing: 2px; text-transform: uppercase;
      color: var(--blue-lt); width: fit-content; margin-bottom: 24px;
      animation: fadeUp .7s ease both;
    }
    .hero-tag::before { content: '●'; font-size: .45rem; color: #60A5FA; animation: pulse 2s infinite; }
    @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.25} }

    .hero-logo-img {
      width: 280px; margin-bottom: 28px;
      animation: fadeUp .7s .1s ease both;
      filter: drop-shadow(0 8px 32px rgba(37,99,235,.5));
    }
    .hero-tagline {
      font-family: 'Fraunces', serif;
      font-size: clamp(1.6rem, 3.5vw, 2.8rem);
      font-weight: 700; line-height: 1.2; color: var(--text);
      animation: fadeUp .7s .2s ease both;
    }
    .hero-tagline em { font-style: italic; color: var(--blue-lt); }
    .hero-sub {
      margin-top: 18px; max-width: 480px;
      font-size: .95rem; line-height: 1.75; color: var(--text-dim);
      animation: fadeUp .7s .3s ease both;
    }
    .hero-actions {
      display: flex; gap: 12px; margin-top: 32px; flex-wrap: wrap;
      animation: fadeUp .7s .4s ease both;
    }
    .btn-primary {
      background: var(--blue); color: var(--white);
      padding: 13px 30px; border-radius: 9px;
      font-weight: 600; font-size: .88rem; text-decoration: none;
      transition: background .2s, transform .15s, box-shadow .2s;
      box-shadow: 0 4px 20px rgba(37,99,235,.4);
    }
    .btn-primary:hover { background: var(--blue-mid); transform: translateY(-2px); box-shadow: 0 8px 28px rgba(37,99,235,.5); }
    .btn-ghost {
      border: 1.5px solid rgba(96,165,250,.35); color: var(--blue-lt);
      padding: 13px 30px; border-radius: 9px;
      font-weight: 500; font-size: .88rem; text-decoration: none;
      transition: border-color .2s, background .2s;
    }
    .btn-ghost:hover { border-color: var(--blue-lt); background: rgba(37,99,235,.1); }

    /* info cards row */
    .hero-cards {
      display: flex; gap: 14px; margin-top: 44px; flex-wrap: wrap;
      animation: fadeUp .7s .5s ease both;
    }
    .hcard {
      background: rgba(255,255,255,.04);
      border: 1px solid rgba(59,130,246,.2);
      border-radius: 12px; padding: 14px 20px;
      backdrop-filter: blur(6px);
    }
    .hcard-label { font-size: .62rem; letter-spacing: 2px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 4px; }
    .hcard-val { font-family: 'Fraunces', serif; font-size: 1.1rem; font-weight: 700; }

    /* ── SECTION BASE ── */
    .section-wrap { padding: 88px 56px; }
    .section-wrap.alt { background: var(--bg2); border-top: 1px solid rgba(59,130,246,.1); }
    .section-label { font-size: .68rem; letter-spacing: 4px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 12px; font-weight: 600; }
    .section-title { font-family: 'Fraunces', serif; font-size: clamp(1.8rem, 3vw, 2.8rem); font-weight: 800; line-height: 1.1; margin-bottom: 48px; }

    /* ── TENTANG ── */
    .tentang-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; align-items: center; }
    .tentang-visual { position: relative; }
    .tentang-frame {
      aspect-ratio: 4/5; border-radius: 20px;
      background: linear-gradient(145deg, var(--bg3), var(--bg2));
      border: 1px solid rgba(59,130,246,.2);
      display: flex; align-items: center; justify-content: center;
      overflow: hidden; position: relative;
    }
    .tentang-frame img { width: 75%; filter: drop-shadow(0 8px 32px rgba(37,99,235,.6)); }
    .tentang-frame::after {
      content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 45%;
      background: linear-gradient(to top, var(--bg2), transparent);
    }
    .tentang-badge {
      position: absolute; bottom: -16px; right: -16px;
      width: 100px; height: 100px; border-radius: 50%;
      background: var(--blue);
      display: flex; flex-direction: column; align-items: center; justify-content: center;
      color: var(--white); box-shadow: 0 8px 28px rgba(37,99,235,.5);
    }
    .tentang-badge-year { font-family: 'Fraunces', serif; font-size: 1.6rem; font-weight: 900; line-height: 1; }
    .tentang-badge-text { font-size: .48rem; letter-spacing: 2px; text-transform: uppercase; font-weight: 700; opacity:.85; }
    .tentang-text p { font-size: .93rem; line-height: 1.8; color: var(--text-dim); margin-bottom: 16px; }
    .stat-row { display: flex; gap: 32px; margin-top: 32px; flex-wrap: wrap; }
    .stat-num { font-family: 'Fraunces', serif; font-size: 2.4rem; color: var(--blue-lt); font-weight: 800; line-height: 1; }
    .stat-label { font-size: .68rem; letter-spacing: 2px; text-transform: uppercase; color: var(--text-dim); margin-top: 3px; }

    /* ── LINIMASA ── */
    .timeline-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); }
    .tl-item {
      padding: 26px 28px; border-left: 1px solid rgba(59,130,246,.2);
      position: relative; transition: background .25s; border-radius: 0 8px 8px 0;
    }
    .tl-item:hover { background: rgba(37,99,235,.07); }
    .tl-item::before {
      content: ''; position: absolute; left: -5px; top: 30px;
      width: 9px; height: 9px; border-radius: 50%;
      background: var(--bg2); border: 2px solid var(--blue-lt);
      transition: background .25s;
    }
    .tl-item:hover::before { background: var(--blue-lt); }
    .tl-step { font-size: .6rem; letter-spacing: 3px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 8px; font-weight: 600; }
    .tl-event { font-family: 'Fraunces', serif; font-size: 1rem; font-weight: 700; margin-bottom: 6px; line-height: 1.3; }
    .tl-date { font-size: .8rem; color: var(--text-dim); }

    /* ── JURI ── */
    .juri-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 24px; margin-top: 48px; }
    .juri-card {
      background: var(--bg3); border: 1px solid rgba(59,130,246,.13);
      border-radius: 16px; overflow: hidden;
      transition: border-color .25s, transform .25s, box-shadow .25s;
    }
    .juri-card:hover { border-color: rgba(59,130,246,.45); transform: translateY(-5px); box-shadow: 0 14px 40px rgba(37,99,235,.18); }
    .juri-photo {
      width: 100%; aspect-ratio: 1; overflow: hidden;
      background: linear-gradient(145deg, #1a2d4a, var(--bg2));
      position: relative;
    }
    .juri-photo img { width: 100%; height: 100%; object-fit: cover; object-position: top; }
    .juri-photo-placeholder {
      width: 100%; height: 100%;
      display: flex; align-items: center; justify-content: center;
      font-size: 4rem;
    }
    .juri-photo::after {
      content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 50%;
      background: linear-gradient(to top, var(--bg3), transparent);
    }
    .juri-info { padding: 18px 20px 22px; }
    .juri-role { font-size: .6rem; letter-spacing: 3px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 5px; font-weight: 600; }
    .juri-name { font-family: 'Fraunces', serif; font-size: 1.1rem; font-weight: 700; margin-bottom: 6px; }
    .juri-pos { font-size: .8rem; color: var(--text-dim); line-height: 1.5; }
    .juri-ketua { border-color: rgba(59,130,246,.35); }
    .juri-ketua .juri-role::before { content: '★ '; color: #FCD34D; }

    /* ── KETENTUAN ── */
    .ket-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin-top: 48px; }
    .ket-card {
      background: var(--bg3); border: 1px solid rgba(59,130,246,.13);
      border-radius: 16px; padding: 28px 28px;
      transition: border-color .25s;
    }
    .ket-card:hover { border-color: rgba(59,130,246,.35); }
    .ket-icon { font-size: 1.8rem; margin-bottom: 14px; }
    .ket-title { font-family: 'Fraunces', serif; font-size: 1.1rem; font-weight: 700; margin-bottom: 14px; color: var(--white); }
    .ket-list { list-style: none; }
    .ket-list li {
      font-size: .85rem; color: var(--text-dim); line-height: 1.6;
      padding: 6px 0; border-bottom: 1px solid rgba(255,255,255,.05);
      display: flex; gap: 10px; align-items: flex-start;
    }
    .ket-list li:last-child { border-bottom: none; }
    .ket-list li::before { content: '→'; color: var(--blue-lt); flex-shrink: 0; font-size: .8rem; margin-top: 2px; }

    /* ── PENGHARGAAN ── */
    .award-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin-top: 48px; }
    .award-card {
      background: var(--bg3); border: 1px solid rgba(59,130,246,.13);
      border-radius: 16px; padding: 28px;
    }
    .award-type { font-size: .68rem; letter-spacing: 3px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 10px; font-weight: 600; }
    .award-name { font-family: 'Fraunces', serif; font-size: 1.15rem; font-weight: 700; margin-bottom: 16px; }
    .award-levels { display: flex; flex-direction: column; gap: 8px; }
    .award-level {
      display: flex; justify-content: space-between; align-items: center;
      padding: 8px 12px; background: rgba(37,99,235,.08);
      border-radius: 8px; font-size: .82rem;
    }
    .award-level span:last-child { color: var(--blue-lt); font-weight: 600; font-size: .75rem; }
    .award-note {
      margin-top: 24px; padding: 18px 20px;
      background: rgba(37,99,235,.08); border-left: 3px solid var(--blue);
      border-radius: 0 10px 10px 0; font-size: .83rem; color: var(--text-dim); line-height: 1.65;
    }

    /* ── CTA DAFTAR ── */
    #daftar {
      padding: 100px 56px; text-align: center; position: relative; overflow: hidden;
    }
    #daftar::before {
      content: ''; position: absolute; inset: 0;
      background: radial-gradient(ellipse 60% 60% at 50% 50%, rgba(37,99,235,.12) 0%, transparent 70%);
    }
    #daftar > * { position: relative; z-index: 1; }
    #daftar .section-title { margin-bottom: 16px; }
    #daftar > p { max-width: 480px; margin: 0 auto 14px; color: var(--text-dim); line-height: 1.75; font-size: .93rem; }
    .daftar-link { font-size: .78rem; color: var(--blue-lt); margin-bottom: 32px; display: block; }
    .daftar-link a { color: var(--blue-lt); }
    .daftar-actions { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; }

    /* ── KONTAK ── */
    .kontak-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 20px; margin-top: 48px; }
    .kontak-card {
      background: var(--bg3); border: 1px solid rgba(59,130,246,.13);
      border-radius: 14px; padding: 24px 24px;
      transition: border-color .25s;
    }
    .kontak-card:hover { border-color: rgba(59,130,246,.35); }
    .kontak-icon { font-size: 1.5rem; margin-bottom: 12px; }
    .kontak-role { font-size: .62rem; letter-spacing: 3px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 5px; font-weight: 600; }
    .kontak-name { font-family: 'Fraunces', serif; font-size: 1rem; font-weight: 700; margin-bottom: 6px; }
    .kontak-detail { font-size: .82rem; color: var(--text-dim); }
    .kontak-detail a { color: var(--blue-lt); text-decoration: none; }

    /* ── FOOTER ── */
    footer {
      background: #060E1C;
      border-top: 1px solid rgba(59,130,246,.1);
      padding: 52px 56px 28px;
    }
    .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 40px; margin-bottom: 48px; }
    .footer-brand img { height: 52px; margin-bottom: 14px; filter: brightness(1.1); }
    .footer-brand p { font-size: .82rem; color: var(--text-dim); line-height: 1.7; }
    .footer-col h4 { font-size: .63rem; letter-spacing: 4px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 16px; font-weight: 600; }
    .footer-col ul { list-style: none; }
    .footer-col li { margin-bottom: 9px; }
    .footer-col a { color: var(--text-dim); text-decoration: none; font-size: .82rem; transition: color .2s; }
    .footer-col a:hover { color: var(--text); }
    .footer-bottom { border-top: 1px solid rgba(255,255,255,.06); padding-top: 24px; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px; }
    .footer-bottom p { font-size: .72rem; color: var(--text-dim); opacity: .55; }
    .social-links { display: flex; gap: 10px; }
    .social-links a {
      width: 34px; height: 34px; border-radius: 8px;
      border: 1px solid rgba(59,130,246,.22);
      display: flex; align-items: center; justify-content: center;
      color: var(--text-dim); font-size: .72rem; text-decoration: none; font-weight: 600;
      transition: background .2s, color .2s, border-color .2s;
    }
    .social-links a:hover { background: rgba(37,99,235,.18); color: var(--blue-lt); border-color: var(--blue-lt); }

    /* ── ANIMATIONS ── */
    @keyframes fadeUp { from { opacity: 0; transform: translateY(22px); } to { opacity: 1; transform: translateY(0); } }
    .reveal { opacity: 0; transform: translateY(22px); transition: opacity .65s ease, transform .65s ease; }
    .reveal.visible { opacity: 1; transform: none; }

    /* ── BERITA ── */
    .berita-grid {
      display: grid;
      grid-template-columns: 1.7fr 1fr 1fr;
      gap: 22px; margin-top: 48px;
    }
    .berita-card {
      background: var(--bg3);
      border: 1px solid rgba(59,130,246,.13);
      border-radius: 16px; overflow: hidden;
      transition: border-color .25s, transform .25s;
      cursor: pointer; text-decoration: none; color: inherit; display: block;
    }
    .berita-card:hover { border-color: rgba(59,130,246,.4); transform: translateY(-4px); }
    .berita-thumb {
      width: 100%; aspect-ratio: 16/9;
      overflow: hidden; position: relative;
      background: var(--bg2);
    }
    .berita-card:first-child .berita-thumb { aspect-ratio: 4/3; }
    .berita-thumb img { width: 100%; height: 100%; object-fit: cover; }
    .berita-thumb-placeholder {
      width: 100%; height: 100%; min-height: 160px;
      display: flex; align-items: center; justify-content: center;
      background: linear-gradient(135deg, #162543, var(--bg2));
      font-size: 2.5rem;
    }
    .berita-card:first-child .berita-thumb-placeholder { font-size: 3.5rem; }
    .berita-body { padding: 18px 20px 22px; }
    .berita-tag { font-size: .6rem; letter-spacing: 3px; text-transform: uppercase; color: var(--blue-lt); margin-bottom: 8px; font-weight: 600; }
    .berita-judul { font-family: 'Fraunces', serif; font-size: .98rem; font-weight: 700; line-height: 1.4; margin-bottom: 8px; color: var(--text); }
    .berita-card:first-child .berita-judul { font-size: 1.25rem; }
    .berita-desc { font-size: .8rem; color: var(--text-dim); line-height: 1.6; margin-bottom: 10px; }
    .berita-date { font-size: .72rem; color: var(--text-dim); opacity: .6; }

    /* scan card special style */
    .berita-scan-img {
      width: 100%; aspect-ratio: 4/3;
      overflow: hidden;
    }
    .berita-scan-img img { width: 100%; height: 100%; object-fit: cover; object-position: center; }

    /* ── MOBILE ── */
    @media (max-width: 900px) {
      nav { padding: 16px 20px; }
      .nav-links, .nav-cta { display: none; }
      .section-wrap { padding: 64px 20px; }
      #hero { padding: 100px 20px 60px; }
      .tentang-grid, .ket-grid, .award-grid { grid-template-columns: 1fr; }
      .berita-grid { grid-template-columns: 1fr; }
      .footer-grid { grid-template-columns: 1fr 1fr; }
      .tugu-deco { display: none; }
      #daftar { padding: 72px 20px; }
    }